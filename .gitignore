# Release output (produced by tools/make-release.ps1; never committed)
发布/

# Optional offline payload: the ~36 MB portable Node zip. The README.txt here
# documents it and IS tracked; the archives themselves are not.
payload/*.zip
payload/*.tar.gz
payload/*.7z

# Local run/debug output
*.log
*.tmp

# npm pack output (dsh-oneclick-x.y.z.tgz). Build release archives with
# `npm pack --pack-destination <dir>` so they never land in the repository root.
*.tgz

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
